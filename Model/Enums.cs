namespace BierPongTurnier.Model
{
    public enum TeamPosition
    {
        FIRST,
        SECOND,
        NONE
    }

    public enum GameResult
    {
        WIN,
        LOSE,
        DRAW,
        OPEN,
        NOT_PARTICIPATED
    }
}